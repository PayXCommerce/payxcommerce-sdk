# Transactions

Lookup a transaction by public transaction reference.

Endpoint:

```text
GET /api/v1/transactions/{transaction_reference}
```

The response includes amount, currency, payment status, gateway name, paid time, request number, and settlement status.

